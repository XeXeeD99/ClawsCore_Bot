services:
  - type: web
    name: claws-core-bot
    env: python
    plan: free
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python clawscore_bot.py"
    envVars:
      - key: BOT_TOKEN
        sync: false
